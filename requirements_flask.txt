flask==2.3.3
werkzeug==2.3.7
opencv-python>=4.8.0
psutil>=5.0
soundfile>=0.12.0
numpy>=1.26.0
scipy>=1.13.0
mediapipe>=0.10.0

# PyTorch (CPU) — install separately if you need GPU:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchaudio>=2.0.0

# Voice cloning (Coqui TTS)
coqui-tts>=0.25.0
coqpit-config>=0.2.0
transformers>=4.43,<5
